import React from "react";
import FullChatScreen from "../chat";

export default function ChatScreen() {
  // Instead of redirecting, render the full chat screen implementation directly
  return <FullChatScreen />;
}
