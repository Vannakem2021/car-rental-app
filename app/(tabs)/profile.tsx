import React from "react";
import FullProfileScreen from "../profile";

export default function ProfileScreen() {
  // Instead of a placeholder, render the full profile screen implementation directly
  return <FullProfileScreen />;
}
